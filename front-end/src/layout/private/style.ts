import { makeStyles } from "@material-ui/core";

const useStyle = makeStyles({
  container: {
    width: '100vw',
    height: '100vh',
  },
  item_body: {
    width: '100%',
    height: '100%',
  },
})

export default useStyle